# 🔍 無料OCRライブラリ依存関係
# 画像ベースOCRパイプライン用

# Primary OCR Engine (高精度日本語対応)
easyocr==1.7.1

# Fallback OCR Engine (軽量・高速)
pytesseract==0.3.10

# 画像処理ライブラリ
Pillow==10.4.0

# その他の依存関係
opencv-python==4.10.0.84
numpy==1.26.4

# インストール手順:
# pip install -r requirements_ocr.txt
#
# macOS追加インストール:
# brew install tesseract tesseract-lang